# CONTEXT — PDF 电子签章与证书管理系统

## 核心实体

- **管理员 (Admin)**：本系统唯一的操作角色，负责为外部业务系统的用户颁发数字证书。仅此角色，无其他角色。
- **签署人 (Signer)**：实际持有证书并使用证书进行签章的实体。分为两类：企业签署人（cert_type=1）和个人签署人（cert_type=2）。`credit_code` 在企业场景下存统一信用代码，在个人场景下存身份证号。签署人身份（姓名、部门等）来自第三方业务系统，不归本系统管理。
- **印章 (Seal)**：可视化的签名/签章图片（透明背景 PNG）。支持两种来源：① 调用方提供 URL（兼容模式）；② 系统根据证书信息自动生成。自动生成规则——企业证书：红色圆形公章，红色圆圈+中心五角星+公司名称沿上半弧分布+底部日期；个人证书：黑色楷体横排姓名+底部日期。日期取印章生成时的当前时间。
- **签章 (Signing)**：将印章图片与密码学数字签名写入 PDF 的完整动作过程。印章是可视资产，签章是动作。
- **数字证书 (X.509 Certificate)**：由内部 CA 签发的身份凭证，包含公私钥对，以 PKCS#12 格式存储。证书有有效期，超期后不可用于签章。签发时指定 `cert_type`（1=企业，2=个人）区分证书类型。一个签署人有且仅有一张当前有效证书。
- **PDF 文档 (Document)**：被签署的对象。可以是普通未签章 PDF，也可以是其他人已经签署过的 PDF。本系统支持多人签章。每次签章不对原文件进行修改，生成新文件 URL 返回给调用方。

## 关联关系

- 签署人 1:1 数字证书（通过 `signer_id` 绑定，同一 `signer_id` 仅一张有效证书，新证书签发时旧证书自动作废）
- PDF 文档 1:N 签章（多人依次签署，同一签署人可签多次）

## 身份识别

- `signer_id`（证书 ID）由系统在签发证书时自动生成，作为证书的唯一标识，写入证书扩展字段并随签发响应一起返回给调用方
- 签发证书时，管理员提供 `credit_code` + 姓名 + 部门，系统生成 `signer_id` 并建立绑定关系
- 签章时调用方传入 `signer_id`，系统自动匹配其当前有效证书
- 调用方无需了解证书序列号等内部概念，但需保管好 `signer_id` 用于后续签章

## 运作模式

- **签章流程：** 调用方传入 PDF 文件 URL，可选传入印章图片 URL。印章图片未提供时，系统根据证书类型和姓名自动生成印章图片（企业红章或个人方章）并嵌入签章位置。系统临时下载 PDF 后执行签章，生成新文件并返回新文件 URL。
- **多签流转：** 后一个签署人传入前一个签署人签名后的文件 URL，依次叠加。并发控制由调用方保证。

## 安全约束

- 证书签发时绑定 `cert_type`（1=企业，2=个人）、`credit_code`（企业为统一信用代码，个人为身份证号）、姓名、部门（可选）
- 印章图片未提供时由系统自动生成，生成规则与 `cert_type` 绑定，保证印章形式与证书类型一致
- 临时下载的 PDF 落盘至本地临时目录，定时清理，不存于内存以保障可溯源

## 证书生命周期

- **签发：** 管理员指定 `cert_type`、`credit_code`、姓名、部门、有效期（默认 365 天，可自定义），系统生成公私钥并签发 X.509 证书，同时自动生成 `signer_id` 返回给调用方
- **撤销：** 两种方式——① 同 `signer_id` 新证签发时旧证自动标记 REVOKED；② 管理员手动吊销
- **过期：** 超过有效期后证书不可再用于签章，系统拒绝签署并提示"证书已过期"
