# CONTEXT — PDF 电子签章与证书管理系统

## 核心实体

- **管理员 (Admin)**：本系统唯一的操作角色，负责为外部业务系统的用户颁发数字证书。仅此角色，无其他角色。
- **签署人 (Signer)**：实际持有证书并使用证书进行签章的实体。签署人身份（姓名、部门等）来自第三方业务系统，不归本系统管理。签章时，签署人信息由调用方随请求一起传入。
- **印章 (Seal)**：可视化的签名/签章图片（透明背景 PNG）。在插入签章和确认签章时，由调用方提供 URL，本系统不负责印章的上传、审批与管理。
- **签章 (Signing)**：将印章图片与密码学数字签名写入 PDF 的完整动作过程。印章是可视资产，签章是动作。
- **数字证书 (X.509 Certificate)**：由内部 CA 签发的身份凭证，包含公私钥对，以 PKCS#12 格式存储。证书有有效期，超期后不可用于签章。一个签署人有且仅有一张当前有效证书。
- **PDF 文档 (Document)**：被签署的对象。可以是普通未签章 PDF，也可以是其他人已经签署过的 PDF。本系统支持多人签章。每次签章不对原文件进行修改，生成新文件 URL 返回给调用方。

## 关联关系

- 签署人 1:1 数字证书（通过 `signer_id` 绑定，同一 `signer_id` 仅一张有效证书，新证书签发时旧证书自动作废）
- PDF 文档 1:N 签章（多人依次签署，同一签署人可签多次）

## 身份识别

- 管理员签发证书时绑定调用方的 `signer_id`
- 签章时调用方传入 `signer_id`，系统自动匹配其当前有效证书
- 调用方无需了解证书编号、序列号等本系统内部概念

## 运作模式

- **签章流程：** 调用方传入 PDF 文件 URL + 印章图片 URL，系统临时下载 PDF 后执行签章，生成新文件并返回新文件 URL。
- **多签流转：** 后一个签署人传入前一个签署人签名后的文件 URL，依次叠加。并发控制由调用方保证。

## 安全约束

- 证书签发时绑定 `signer_id`（统一信用代码）+ 姓名 + 部门，签章时校验一致性
- 印章图片与证书主体名称的一致性由调用方负责，本系统不做校验
- 临时下载的 PDF 落盘至本地临时目录，定时清理，不存于内存以保障可溯源

## 证书生命周期

- **签发：** 管理员指定 `signer_id`、姓名、部门、有效期（默认 365 天，可自定义），系统生成公私钥并签发 X.509 证书
- **撤销：** 两种方式——① 同 `signer_id` 新证签发时旧证自动标记 REVOKED；② 管理员手动吊销
- **过期：** 超过有效期后证书不可再用于签章，系统拒绝签署并提示"证书已过期"
