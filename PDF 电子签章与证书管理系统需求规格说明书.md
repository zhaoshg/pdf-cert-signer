# PDF 电子签章与证书管理系统 需求规格说明书 (PRD)

| **项目名称** | **PDF 电子签章与证书管理系统** | **版本号** | **v2.0**                  |
| ------------ | ------------------------------ | ---------- | ------------------------- |
| **文档作者** | AI 架构师                      | 日期       | 2026-07-31                |
| **文档状态** | [Draft] 拟定中                 | 目标读者   | 产品、前端、后端、QA 团队 |

> v2.0 变更说明：基于领域建模澄清，明确了系统边界、用户角色划分、签署人身份模型、签章流转机制及证书生命周期。详细变更见各章节。

## 一、 项目背景与业务目标

### 1.1 项目背景

在企业数字化办公场景中，需要一套自主可控、合规防篡改的 PDF 电子签章系统。本系统作为**证书颁发与签章服务**，嵌入到第三方业务系统的签章流程中：

- **管理员（本系统角色）** 为业务系统的用户签发 X.509 数字证书
- **签署人** 的身份（姓名、部门等）来自第三方业务系统，不归本系统管理
- 调用方传入 PDF 和印章，本系统完成密码学签章并返回新文件

### 1.2 核心目标

1. **证书全生命周期管理：** 提供证书签发、查看、下载、吊销功能，支持通过 `signer_id` 绑定外部业务系统用户身份。
2. **可视化 PDF 签章交互：** 提供前端 PDF 预览、动态放置印章图片、拖拽缩放定位、未提交前"清除印章"撤销功能。
3. **安全防篡改签章引擎：** 确认签章后，后端结合**签署人私钥证书 + 印章图片 + RFC 3161 可信时间戳**对 PDF 进行 PAdES 密码学签名，确保签章后文件不可篡改，且在标准阅读器（如 Adobe Acrobat）中可验证信任。

## 二、 领域模型

### 2.1 核心实体

| 实体 | 定义 |
|------|------|
| **管理员 (Admin)** | 本系统唯一的操作角色，负责为外部业务系统的签署人颁发数字证书。仅此角色。 |
| **签署人 (Signer)** | 实际持有证书并使用证书进行签章的实体。身份（姓名、部门等）来自第三方业务系统，通过 `signer_id`（统一信用代码）标识。 |
| **印章 (Seal)** | 可视化的签章图片（透明背景 PNG），在签章时由调用方提供 URL。本系统**不负责**印章的上传、审批与管理。 |
| **签章 (Signing)** | 将印章图片与密码学数字签名写入 PDF 的完整动作。印章是可视资产，签章是动作。 |
| **数字证书 (X.509 Certificate)** | 由内部 CA 签发的身份凭证，含公私钥对，PKCS#12 格式存储。同一 `signer_id` 仅一张有效证书。 |
| **PDF 文档 (Document)** | 被签署的对象。支持多人先后签署。每次签章生成新文件 URL，不对原文件进行修改。 |

### 2.2 关联关系

```
签署人 (signer_id) ──── 1:1 ──── 数字证书
     │
     └─────── 1:N ──────── 签章 ──────── N:1 ──────── PDF 文档
```

- 签署人通过 `signer_id` 绑定证书。同一 `signer_id` 仅一张有效证书，签发新证时旧证自动作废。
- PDF 文档支持多人依次签署（多签），同一签署人可在同一份 PDF 上签多次。
- 印章由调用方提供，不与证书或签署人建立本系统内的绑定关系。

### 2.3 系统边界

```
┌──────────────────────────────────────────────────────┐
│              第三方业务系统（调用方）                    │
│  - 管理签署人身份（姓名、部门）                          │
│  - 管理印章图片上传与审批                               │
│  - 控制签章顺序与并发锁                                │
│  - 传入 PDF URL + 印章 URL + signer_id 发起签章         │
└──────────────────────┬───────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│              本系统（证书 + 签章服务）                    │
│  - 证书签发、查看、下载、吊销                          │
│  - PDF 临时下载 → 密码学签名 → 生成新文件 URL          │
│  - 文件存储（OBS / 本地磁盘）                          │
│  - 审计日志                                           │
└──────────────────────────────────────────────────────┘
```

## 三、 系统架构与模块划分

```
[ 客户端 (Web UI) ]
   ├── 模块 1：证书管理页面（签发/查看/下载/吊销）
   └── 模块 2：PDF 签章画布页面（PDF.js 预览 + 印章拖拽/清除/确认）

            ▼
[ 后端 API (Spring Boot) ]
   ├── 模块 3：内部 CA 证书服务（BouncyCastle / EU DSS PAdES）
   ├── 模块 4：PDF 密码学签章引擎（下载 PDF → 签名 → 上传新文件）
   └── 模块 5：文件存储适配层（腾讯云/七牛云/阿里云 OBS + 本地磁盘）
```

## 四、 详细功能需求

### 模块 1：证书颁发与管理 (Certificate Management)

#### 1.1 证书签发

**触发方：** 管理员

**输入参数：**

| 参数 | 说明 | 必填 | 默认值 |
|------|------|------|--------|
| credit_code | 签署人统一信用代码 | 是 | — |
| `name` | 签署人姓名 | 是 | — |
| `department` | 签署人部门 | 是 | — |
| `email` | 签署人邮箱 | 否 | — |
| `valid_days` | 证书有效天数 | 否 | 365 |
| `key_algorithm` | 密钥算法 | 否 | RSA 2048 |

**处理逻辑：**

1. 校验 `credit_code` 格式，生成公私钥对。
2. 后端使用内部根证书（Root CA）签发 X.509 证书，CN 格式如 `张三(财务部)`。
3. 生成 PKCS#12 格式（`.p12`）密钥库，AES-256 加密存储。
4. **自动作废：** 若该 `signer_id` 已有 ACTIVE 证书，先将其状态置为 REVOKED，再存储新证书。
5. 数据库记录： `signer_id`,`credit_code`, `name`, `department`, `serial_number`, `valid_from`, `valid_to`, `cert_status` (ACTIVE / REVOKED)。

#### 1.2 证书查看

证书列表展示： 证书Id（`signer_id`）,证书主题 (CN)、颁发者、序列号、有效期起止、签署人姓名、部门、状态 (ACTIVE / REVOKED)。支持按 `credit_code`、姓名、状态筛选。

#### 1.3 证书下载

- **下载私钥证书 (`.p12`)**：用于本地备份或导入其他加密设备。下载时需二次校验密码。
- **下载根证书 (`RootCA.crt`)**：提供企业根证书下载，附带提示："请将此根证书导入至本地电脑受信任的根证书颁发机构，以消除 Adobe Reader 中的黄色警告"。

#### 1.4 证书吊销

**触发方：** 管理员

管理员在证书列表中找到目标证书，点击"吊销"，确认后证书状态置为 REVOKED。已吊销的证书不可再用于签章。

**撤销方式汇总：**

| 方式 | 触发条件 | 说明 |
|------|----------|------|
| 自动作废 | 同一 `credit_code` 签发新证书 | 旧证自动标记 REVOKED |
| 手动吊销 | 管理员主动操作 | 如签署人离职等场景 |

### 模块 2：PDF 预览与可视化印章拖拽 (PDF Visual Signer)

#### 2.1 PDF 文件加载与预览

- **技术方案：** 基于 PDF.js 在 Web 端渲染 PDF 文件。
- **输入：** 调用方传入 PDF 文件 URL。
- **功能要求：** 支持多页翻页、放大/缩小、自适应宽度展示、显示总页数及当前页码。

#### 2.2 插入印章（未确认状态）

- **触发方式：** 点击工具栏 **"插入印章"** 按钮。
- **印章来源：** 印章图片 URL 由调用方在点击插入时传入（来自业务系统的印章管理）。
- **交互表现：**
  - 印章图片以可拖拽浮层（DOM / Canvas Overlay）形式叠加在当前预览的 PDF 页面中央。
  - **拖拽与缩放：** 鼠标按住拖拽至任意位置；拖拽四周控制点可按比例拉伸/缩小。
  - **页面绑定：** 记录目标页码 (`pageIndex`) 及相对于 PDF 实际尺寸的坐标 (`x, y, width, height`)。
  - **多印章支持：** 同一签署人可在 PDF 上多次点击"插入印章"，放置多枚印章在不同位置（如同一页页脚和正文各一枚，或不同页各一枚）。所有印章统一在"确认签章"时一起固化。

#### 2.3 清除印章（未确认状态）

- **触发方式：** 点击工具栏 **"清除印章"** 按钮，或印章浮层右上角删除图标。
- **处理逻辑：** 仅清理前端尚未提交固化的临时印章 DOM 节点，恢复 PDF 页面至原始预览状态，清空暂存坐标及页码数据。

#### 2.4 确认签章（提交固化）

**触发方式：** 用户调整位置后，点击工具栏 **"确认签章"** 按钮。

**二次确认弹窗：** 提示"即将使用您的数字证书 [签署人姓名] 签署此文档，签署后文档将产生防篡改效力，是否继续？"

**提交参数（发送至后端）：**

```json
{
  "signerId": "USCI_1234567890",
  "pdfUrl": "https://obs.example.com/path/to/document.pdf",
  "signatures": [
    {
      "pageIndex": 1,
      "sealUrl": "https://obs.example.com/seals/finance_seal.png",
      "x": 150.5,
      "y": 620.0,
      "width": 120.0,
      "height": 120.0,
      "reason": "部门业务审批确认"
    }
  ]
}
```

**前端反馈：** 界面进入 Loading 状态，禁用所有操作按钮。签署成功后重新加载后端返回的签名后 PDF URL，并展示"签署成功！证书已验真"的绿色 Banner。

### 模块 3：后端密码学签章与时间戳引擎 (Backend Signing Engine)

#### 3.1 PDF 下载与临时存储

1. 后端接收 `pdfUrl`，从调用方提供的 URL 下载 PDF 文件。
2. 落盘至本地临时目录（非内存），保障可溯源。
3. 临时目录配置定时清理策略（如保留 24 小时后自动清除）。

#### 3.2 签署人身份校验

1. 根据 `signerId` 查询当前 ACTIVE 状态的证书。
2. 若证书不存在 → 返回错误"未找到有效证书"。
3. 若证书已过期 → 返回错误"您的数字证书已过期，请先重新申请/更新证书"，拒绝签署。
4. 若证书已被吊销 → 返回错误"您的数字证书已被吊销"，拒绝签署。

#### 3.3 坐标换算

前端 PDF.js 根据缩放比例渲染，其像素坐标需转换为 PDF 标准坐标系（Point，1pt = 1/72 inch）。后端根据 PDF 原始页面物理尺寸，将前端传入的坐标换算为 PDF 左下角原点坐标系下的物理坐标 `(X, Y, W, H)`。

#### 3.4 密码学硬签名 (PAdES Standard)

1. **加载签名凭据：** 提取签署人私钥与 X.509 证书链。
2. **下载印章图片：** 从 `sealUrl` 下载印章图片。
3. **外观合成：** 在目标页指定物理坐标处，将印章图片写入 PDF 外观字典。
4. **哈希计算与签名：** 提取排除签名字典后的 PDF 字节流，计算 SHA-256 哈希，使用签署人私钥对哈希进行加密。
5. **可信时间戳嵌入（TSA）：** 向 RFC 3161 时间戳服务器请求时间戳 Token，写入签名字典。
6. **保存与输出：** 生成最终签名 PDF 文件，上传至对象存储，返回新文件 URL。

#### 3.5 文件存储

签署后的文件上传至对象存储，支持：

- 腾讯云 COS
- 七牛云 Kodo
- 阿里云 OSS
- 本地磁盘目录（并行文件系统挂载场景）

签名后的文件**不自动删除**，长期保留。

### 模块 4：审计日志

签章服务每次操作记录以下审计信息：

| 字段 | 说明 |
|------|------|
| `signer_id` | 证书id             |
| credit_code | 签署人统一信用代码 |
| `cert_serial_number` | 证书序列号 |
| `pdf_hash` | 签名前 PDF 哈希值 |
| `signed_pdf_hash` | 签名后 PDF 哈希值 |
| `sign_time` | 签署时间 |
| `client_ip` | 客户端 IP |
| `tsa_response` | 时间戳响应摘要 |

## 五、 接口 (API) 设计

| 接口路径 | 请求方式 | 说明 |
|----------|----------|------|
| `/api/v1/cert/issue` | `POST` | 管理员签发 X.509 数字证书 |
| `/api/v1/cert/list` | `GET` | 查看证书列表，支持按 `credit_code`、姓名、状态筛选 |
| `/api/v1/cert/revoke` | `POST` | 管理员手动吊销指定证书 |
| `/api/v1/cert/download` | `GET` | 下载用户凭证 (`.p12`) 或根证书 (`.crt`) |
| `/api/v1/pdf/sign` | `POST` | 传入 PDF URL、印章 URL、`signerId`、坐标参数，执行密码学签章，返回新文件 URL |

### `/api/v1/cert/issue` 请求示例

```json
{
  "creditCode": "1234567890",
  "name": "张三",
  "department": "财务部",
  "email": "zhangsan@example.com",
  "validDays": 365
}
```

### `/api/v1/pdf/sign` 请求示例

```json
{
  "signerId": "USCI_1234567890",
  "pdfUrl": "https://obs.example.com/path/contract.pdf",
  "signatures": [
    {
      "pageIndex": 1,
      "sealUrl": "https://obs.example.com/seals/finance_seal.png",
      "x": 150.5,
      "y": 620.0,
      "width": 120.0,
      "height": 120.0,
      "reason": "部门业务审批确认"
    }
  ]
}
```

### `/api/v1/pdf/sign` 响应示例

```json
{
  "code": 0,
  "message": "签署成功",
  "data": {
    "signedPdfUrl": "https://obs.example.com/signed/contract_signed_20260731_001.pdf",
    "certSubject": "CN=张三(财务部)",
    "signTime": "2026-07-31T14:30:00+08:00"
  }
}
```

## 六、 业务流程

```
第三方业务系统                        本系统
     │                                  │
     ├─ 1. 传入 PDF URL + 印章 URL ────→│
     │    + signerId + 坐标              │
     │                                  ├─ 2. 校验 signerId → 查有效证书
     │                                  ├─ 3. 下载 PDF 至临时目录
     │                                  ├─ 4. 下载印章图片
     │                                  ├─ 5. 坐标换算（像素→Point）
     │                                  ├─ 6. 请求 TSA 时间戳
     │                                  ├─ 7. 写入签名字典 (PAdES)
     │                                  ├─ 8. 上传签名文件至 OBS
     │                                  ├─ 9. 记录审计日志
     │                                  │
     │←── 10. 返回签名后 PDF URL ────────┤
     │                                  │
     ├─ 11. （多签场景）传入上次签名 ────→│  重复步骤 2-10
     │     PDF URL，继续签署              │
```

**多签流转说明：** 后一个签署人传入前一个签署人签名后的 PDF URL，依次叠加签章。并发控制和签署顺序由调用方保证，本系统仅对传入的文件进行签章。

## 七、 非功能性需求 (NFR)

### 7.1 安全性需求

1. **私钥保护：** `.p12` 证书私钥在服务器端必须 AES-256 加密存储，禁止明文落盘。
2. **防篡改保障：** 符合 PAdES 标准。签署后修改 PDF 任意字符，Adobe Reader 须能自动感应并弹出红色"文档已被篡改"警告。
3. **审计日志：** 所有签章操作完整记录，可追溯。
4. **身份校验：** 签章时校验请求中的签名参数与证书绑定的 `signer_id` + 姓名 + 部门一致性。
5. **印章不校验：** 印章图片与证书主体名称的一致性由调用方负责，本系统不做校验。

### 7.2 存储需求

1. **签署文件存储：** 支持腾讯云 COS、七牛云 Kodo、阿里云 OSS 及本地磁盘目录，通过配置切换。
2. **签署文件保留：** 签名后的 PDF 文件不自动删除，长期保留。
3. **临时文件清理：** 处理过程中下载的临时 PDF 落盘至本地临时目录，定时清理（如保留 24 小时）。

### 7.3 性能与体验需求

1. **PDF 渲染：** 50 页以内、20MB 以内的 PDF，首屏加载时间 < 1.5 秒。
2. **签章响应：** 点击"确认签章"到返回签名后 PDF URL，全过程延迟 < 1.5 秒（并发 50 QPS）。

## 八、 异常场景与处理逻辑

1. **证书过期：** 签章时若证书超期，拒绝签署并返回"您的数字证书已过期，请先重新申请/更新证书"。
2. **证书已吊销：** 签章时若证书状态为 REVOKED，拒绝签署并返回"您的数字证书已被吊销"。
3. **无有效证书：** 若 `signerId` 对应的签署人无 ACTIVE 证书，返回"未找到有效证书，请联系管理员签发"。
4. **身份信息不一致：** 若签章请求中的姓名/部门与证书绑定的信息不一致，拒绝签署并返回具体不匹配字段。
5. **TSA 断连：** 时间戳服务器连接超时，可配置策略：降级为本地可信时间（返回警告）或中断签署报错。
6. **印章图片下载失败：** 若从 `sealUrl` 下载印章图片失败（URL 无效、超时等），返回"印章图片下载失败：xxx"。
7. **PDF 源文件不可达：** 若从 `pdfUrl` 下载 PDF 失败，返回"PDF 文件下载失败：xxx"。
8. **OBS 上传失败：** 签名后文件上传 OBS 失败，记录错误日志并返回"文件上传失败，请联系管理员"。
9. **未确认前关闭页面：** 用户拖拽了印章但未点击"确认签章"直接关闭浏览器，系统不保存任何修改，原始 PDF 保持不变。
10. **并发冲突（调用方兜底）：** 若调用方未做好并发控制，同一文件可能被多次签章——建议调用方在业务侧实施全局锁，本系统不做并发拦截。
