# PDF 电子签章与证书管理系统 需求规格说明书 (PRD)

| **项目名称** | **PDF 电子签章与证书管理系统** | **版本号** | **v1.0**                  |
| ------------ | ------------------------------ | ---------- | ------------------------- |
| **文档作者** | AI 架构师                      | 日期       | 2026-07-31                |
| **文档状态** | [Draft] 拟定中                 | 目标读者   | 产品、前端、后端、QA 团队 |

## 一、 项目背景与业务目标

### 1.1 项目背景

在企业数字化办公场景中，需要一套自主可控、合规防篡改的 PDF 电子签章系统。系统需具备为企业员工/部门独立颁发 X.509 数字证书的能力，并允许用户在 Web 端可视化预览 PDF，将个人/部门印章图片拖拽至任意位置后，使用自身的数字证书对 PDF 完成**符合密码学标准的硬签名**。

### 1.2 核心目标

1. **证书全生命周期管理：** 提供证书在线申请、签发、查看及标准格式（如 `.p12` / `.pfx` 或 `.crt`）的下载功能。
2. **可视化 PDF 签章交互：** 提供前端 PDF 预览、动态绘制/放置图片印章、拖拽缩放定位、未提交前“一键清除撤销”功能。
3. **安全防篡改签章引擎：** 确认签章后，后端结合**签署人私钥证书 + 印章图片 + RFC 3161 可信时间戳**对 PDF 进行 PAdES 密码学签名，确保签章后文件不可篡改，且在标准阅读器（如 Adobe Acrobat）中可验证信任。

## 二、 系统架构与模块划分

系统整体划分为四大核心模块：

Plaintext

```
[ 客户端 (Web UI) ]
   ├── 模块 1：证书管理页面 (申请/展示/下载)
   └── 模块 2：PDF 签章画布页面 (PDF.js 预览 + 印章拖拽/清空/确认)
            │
            ▼
[ 后端 API (Spring Boot / Python Engine) ]
   ├── 模块 3：内部 CA 证书颁发服务 (BouncyCastle / PKI)
   └── 模块 4：PDF 密码学签章引擎 (PDFBox / pyHanko + TSA)
```

## 三、 详细功能需求

### 模块 1：证书颁发与下载管理 (Certificate Management)

#### 1.1 证书申请与生成

- **业务描述：** 系统管理员或系统根据用户实名身份（用户 ID、姓名、部门、邮箱），自动或手动触发颁发一张专属的 X.509 数字证书。
- **输入参数：** 用户 ID、证书使用者名称（Common Name, CN，如 `张三(财务部)`）、有效期（默认 365 天）、密钥长度（RSA 2048 位或 SM2 国密）。
- **处理逻辑：**
  1. 后端调用内部根证书（Root CA）对用户生成公私钥对并签名。
  2. 生成 PKCS#12 格式（`.p12` 或 `.pfx`）密钥库，并使用安全强密码（或用户自定义密码）加密私钥。
- **数据库记录：** 记录 `cert_id`, `user_id`, `serial_number`, `valid_from`, `valid_to`, `cert_status` (ACTIVE/REVOKED)。

#### 1.2 证书查看与下载

- **前端展示：** 用户个人中心展示当前证书信息（证书主题、颁发者、序列号、有效期起止）。
- **下载功能：**
  - **下载私钥证书 (`.p12` / `.pfx`)：** 用于本地备份或导入其他加密设备，下载时需二次校验密码。
  - **下载根证书 (`RootCA.crt`)：** 提供企业根证书下载按钮，并提供提示：“*请将此根证书导入至本地电脑受信任的根证书颁发机构，以消除 Adobe Reader 中的黄色警告*”。

### 模块 2：PDF 预览与可视化拖拽签章 (PDF Visual Signer)

#### 2.1 PDF 文件加载与预览

- **技术方案：** 基于 `PDF.js` 在 Web 端渲染 PDF 文件。
- **功能要求：** 支持多页翻页、放大/缩小、自适应宽度展示、显示总页数及当前页码。

#### 2.2 插入签章（未确认状态）

- **触发方式：** 点击页面工具栏的 **“插入签章”** 按钮。
- **印章来源：**
  1. 优先获取用户预先设置的默认电子印章/手写签名图片（透明背景 `.png`）。
  2. 若无默认印章，弹出“选择/上传印章”弹窗供用户临时上传。
- **交互表现（核心）：**
  - 点击“插入签章”后，印章图片以可拖拽浮层（DOM / Canvas Overlay）形式叠加在当前预览的 PDF 页面中央。
  - **拖拽与缩放：** 用户可通过鼠标按住印章拖拽至页面任意位置；拖拽印章四周的控制点可按比例拉伸/缩小印章尺寸。
  - **页面绑定：** 记录印章放置的目标页码 (`pageIndex`) 以及基于该页 PDF 实际尺寸的相对坐标（`x, y, width, height`）。

#### 2.3 清除签章（未确认状态）

- **触发方式：** 点击工具栏的 **“清除签章”** 按钮，或印章浮层右上角的“X”删除图标。
- **处理逻辑：**
  - 仅清理前端画布/页面上**尚未提交给后端做密码学固化的临时印章 DOM 节点**。
  - 恢复 PDF 页面至原始未更改的预览状态，清空暂存的坐标及页码数据。

#### 2.4 确认签章（提交固化）

- **触发方式：** 用户调整好位置和大小后，点击工具栏 **“确认签章”** 按钮。

- **二次确认弹窗：** 提示“*即将使用您的数字证书 [张三] 签署此文档，签署后文档将产生防篡改效力，是否继续？*”。

- **提交参数（发送至后端）：**

  JSON

  ```
  {
    "documentId": "pdf_doc_10086",
    "pageIndex": 1,
    "x": 150.5,
    "y": 620.0,
    "width": 120.0,
    "height": 120.0,
    "reason": "部门业务审批确认"
  }
  ```

- **前端反馈：** 界面进入 Loading 状态，禁用所有操作按钮。签署成功后重新加载后端返回的签名后 PDF，并展示“签署成功！证书已验真”的绿色 Banner。

### 模块 3：后端密码学签章与时间戳引擎 (Backend Signing Engine)

#### 3.1 坐标换算（前端像素坐标 $\rightarrow$ PDF 点阵坐标）

- **逻辑：** 由于前端 PDF.js 缩放比例（Zoom Ratio）不同，后端接收到坐标后，必须根据 PDF 原始页面物理尺寸（Point，1 pt = 1/72 inch），将前端传入的相对百分比或像素坐标转换为 PDF 标准下左角原点坐标系的物理坐标 $(X, Y, W, H)$。

#### 3.2 密码学硬签名 (PAdES Standard)

1. **加载签名者凭据：** 根据当前登录用户 ID，提取其绑定的私钥与 X.509 证书链（`.p12`）。
2. **外观合成：** 在目标页指定物理坐标处，将用户的透明背景印章图片写入 PDF 外观字典（`AcroForm / Signature Field`）。
3. **哈希计算与私钥加密：** * 提取排除签名字典后的 PDF 字节流（`ByteRange`），计算 SHA-256 哈希。
   - 使用用户私钥对哈希进行 RSA/SM2 加密。
4. **可信时间戳嵌入（TSA）：**
   - 向指定的 RFC 3161 时间戳服务器（内网自建 TSA 或 CFCA 等商业第三方）发送哈希请求。
   - 将返回的 `TimeStampToken` 塞入签名字典，确保签署时间不可抵赖。
5. **保存与输出：** 生成最终包含数字签名、印章外观与时间戳的增量 PDF 文件。

## 四、 非功能性需求 (NFR)

### 4.1 安全性需求

1. **私钥保护：** 用户的 `.p12` 证书私钥在服务器端必须采用 AES-256 加密存储，禁止明文落盘。
2. **防篡改保障：** 必须符合 PAdES 标准，任何人在签署后修改 PDF 任意字符，Adobe Reader 必须能自动感应并弹出红色“文档已被篡改”警告。
3. **审计日志：** 签章服务需记录审计日志，包括：`签署人 ID`、`证书序列号`、`文档 Hash`、`签署时间`、`客户端 IP`、`时间戳响应摘要`。

### 4.2 性能与体验需求

1. **渲染流畅度：** 前端渲染 50 页以内、20MB 以内的 PDF 文件，首屏加载时间小于 1.5 秒。
2. **签章响应时间：** 点击“确认签章”到后端完成哈希计算、时间戳获取及文件写盘并返回结果，全过程延迟应 **$< 1.5$ 秒**（并发 50 QPS 场景下）。

## 五、 主要页面流程图 (Flowchart)

Plaintext

```
[ 用户打开 PDF 签章页面 ]
          │
          ▼
    [ 加载并预览 PDF ]
          │
          ├──────────────► [ 点击 "清除签章" ] ──► [ 移除非固化 DOM 节点，重置状态 ]
          │
    [ 点击 "插入签章" ]
          │
          ▼
  [ 页面中央渲染印章浮层 ]
          │
    [ 拖拽 / 缩放至目标位置 ]
          │
    [ 点击 "确认签章" ]
          │
          ▼
   [ 后端执行密码学硬签名 ]
   ├── 1. 坐标与页面换算
   ├── 2. 加载用户证书私钥
   ├── 3. 请求 RFC 3161 时间戳
   └── 4. 写入 PDF 签名字典 (PAdES)
          │
          ▼
   [ 重新渲染带防篡改绿标的 PDF ]
```

## 六、 接口（API）设计摘要

| **接口路径**            | **请求方式** | **说明**                                   |
| ----------------------- | ------------ | ------------------------------------------ |
| `/api/v1/cert/issue`    | `POST`       | 为指定用户颁发 X.509 数字证书              |
| `/api/v1/cert/download` | `GET`        | 下载用户凭证 (`.p12`) 或根证书 (`.crt`)    |
| `/api/v1/seal/default`  | `GET`        | 获取当前用户的默认签名印章图片             |
| `/api/v1/pdf/sign`      | `POST`       | 提交签章坐标与参数，执行后端密码学数字签名 |

## 七、 异常场景与处理逻辑

1. **证书失效/过期：** 若用户证书已超过有效期，点击“确认签章”时提示“*您的数字证书已过期，请先重新申请/更新证书*”，拒绝签署。
2. **时间戳服务（TSA）断连：** 若后台连接时间戳服务器超时，系统应可配置策略：降级为服务器本地可信时间（提示警告）或中断签署报错。
3. **未确认前关闭页面：** 用户插入并拖拽了印章，但未点击“确认签章”直接关闭浏览器，系统不保存任何修改，原始 PDF 保持不变。